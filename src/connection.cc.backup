#include "connection.h"
#include <iostream>

namespace coap {

connection::connection(std::string address, int port): _address(address), _port(port), _state(connection::DISCONNECTED)
{
	std::cout << "connection constructor" << std::endl; 
	_length = BUFFER_MAX_SIZE;
	_buffer = new uint8_t [_length];
}	

connection::~connection()
{
	std::cout << "connection destructor" << std::endl; 
	delete [] _buffer;
}

bool connection::establish()
{
	return false;
}

bool connection::disconnect()
{
	return false;
}

bool connection::send()
{
	return false;
}

bool connection::receive()
{
	return false;
}

}//coap


